package cm.vogt.digitalcampus.common.enums;

/** Statut de publication generique — News, Event, Program, Page CMS, etc. */
public enum PublicationStatus {
    DRAFT,
    SCHEDULED,
    PUBLISHED,
    ARCHIVED
}
